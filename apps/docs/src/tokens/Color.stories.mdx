import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs';

import { color } from '@triprint/tokens';

import { filterColor } from '../utils';

<Meta title="Tokens/Color" />

# Color

<ColorPalette>
  <ColorItem title="Primary" colors={filterColor(color, 'primary')} />
  <ColorItem title="Background" colors={filterColor(color, 'background')} />
  <ColorItem title="Surface" colors={filterColor(color, 'surface', 'inverse')} />
  <ColorItem title="Outline" colors={filterColor(color, 'outline')} />
  <ColorItem title="Error" colors={filterColor(color, 'error')} />
</ColorPalette>
