export {
  styled,
  css,
  theme,
  createTheme,
  getCssText,
  globalCss,
  keyframes,
  config,
  reset,
} from './stitches.config';
export type { TriprintCSS } from './stitches.config';

export * from './globals';
